# photobiologyFlecks (development version)

# photobiologyFlecks 0.1.0

* Add function `split_chunks()`.
* Add functions `denoise_chunks()` and `denoise_diffs()`.
* Add functions `summarize_chunks()` and `summarize_chunk()`.

